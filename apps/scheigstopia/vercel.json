{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "framework": "nextjs",
    "installCommand": "pnpm install --frozen-lockfile",
    "buildCommand": "pnpm nx build scheigstopia",
    "outputDirectory": ".next",
    "git": {
        "deploymentEnabled": {
            "main": false
        }
    }
}
